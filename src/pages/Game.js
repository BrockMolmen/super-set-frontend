import React from 'react'

const Game = () => {
  return (
    <div>
      <h1>GAME</h1>
      {/* api call to GiantBomb game page, get box art, screencap, details ( need to remove <p><h1> tags), 
      and have a favorite or remove for set button depending if they have the game saved */}
      {/* pull prices from the PC API loose, CIB, New */}
    </div>
  )
}

export default Game
